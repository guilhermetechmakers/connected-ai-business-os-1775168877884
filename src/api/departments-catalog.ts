/** Department workspace directory — Edge Function via native fetch wrapper in lib. */
export {
  createTenantDepartmentWorkspace,
  fetchDepartmentWorkspaceListCatalog,
  parseWorkspaceDepartmentList,
  validateDepartmentShape,
} from "@/lib/departments-workspace-list-api";
